.env

# logs and shell-redirect artifacts
*.log
/echo
/file.log

# scratch/backup area (held copied File History catalogs, now removed)
/file_history/

# VM_status is its own separate git repo — do not fold it into this one
/VM_status/

# accidental Export-Alias output from show_edge_workspaces.ps1
/Show-Edge-Workspaces

# local Pester cache created by Invoke-Tests.ps1 when Pester 5+ isn't installed
/.pester/
